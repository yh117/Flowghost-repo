<h1>Listing courses</h1>
<%= params.inspect %>
<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <!--th>Id</th>-->
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
	<td><h3><%= course.name %></h3></td>
	<!--td>%= course.id %></td>-->
	<td><%= link_to 'Show', course, {:class=>"btn btn-success"} %></td>
  <td><%= link_to 'Show II', gradeview_path(:isSample=>"yes", :course_id=>course.id), {:class=>"btn btn-success"} %></td>
	<td>
	  <!-- <a class="dropdown-toggle" data-toggle="dropdown" href="#">Edit <b class="caret"></b></a>-->

	  <!-- <ul id="drop1" class="dropdown-menu" role="menu">-->

	    <li><%= link_to 'Edit Assignments', assignments_path(:course_id => course.id), {:class => "btn btn-primary"} %></li><br>

	    <li><%= link_to 'Edit Tags', tags_path(:course_id => course.id), {:class => "btn btn-primary"} %></li><br>

	    <li><%= link_to 'Edit Students', students_path(:course_id => course.id), {:class => "btn btn-primary"} %></li><br>
	    <!-- </ul>-->
	</td>
	<td><%= button_to "Destroy", {:controller => :courses, :action => 'destroy', :id => course.id}, :class=>"btn btn-danger" ,:method => :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Course', new_course_path, {:class=>"btn btn-lg btn-link"} %>
