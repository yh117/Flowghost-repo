<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html; charset=UTF-8">
  <title>FlowChartList</title>
  
  <script type='text/javascript' src='http://cdn.staticfile.org/jquery/1.11.1/jquery.js'></script>
  
  <style type='text/css'>
    dl dt{background:#ccc;padding:5px;}
    dl li{padding:5px;list-style: none;}
    p{height:100px;}
    html,body{margin:0;padding:0;}
  </style>
  


<script type='text/javascript'>//<![CDATA[ 
$(function(){
    //$('dl').groupList();      
    $('dl').groupList(true);
});
$.fn.groupList = function(selfScroll){
     var list = this;
    var listTopOffset = list.offset().top;
    var titleHeight = $(list).find('dt:first').height();

    var currentContext = selfScroll ? list : window;
    if(selfScroll){
        list.css({
            'height':$(window).height() - listTopOffset,
            'overflow':'auto'
        });
    }
    $(currentContext).on('scroll',function(){
        if($(currentContext).scrollTop()-(selfScroll ? 0 : listTopOffset) < titleHeight){
            clearFixed();
        }else{
            var lis = list.find('li');
            for(var i = 0, len = lis.length ; i < len ; i++){
                if(isOnSight(lis[i])){
                    clearFixed().filter('.clone').remove();
                    lis.eq(i).parent().parent().prev().clone(true).addClass('clone').css({
                        'top': selfScroll ? listTopOffset : 0,
                        'position':'fixed',
                        'width':'100%'
                    }).appendTo(list);
                    break;
                }
            }
        }
    });
    var isOnSight = function(item){
        return selfScroll ? listTopOffset < $(item).offset().top + $(item).outerHeight() : $(currentContext).scrollTop() <  $(item).offset().top + $(item).outerHeight();
    }
    var clearFixed = function(){
        return $(list).find('dt').css({
            'position':''
        });
    }
}
//]]>  

</script>


</head>
<body>
    <h1 align="center">Links to students' FlowChart</h1>
    <hr/>
  <dl>
    <dt>A</dt>
    <dd>
        <ul>
			<% @course.students.each do |s| %>
				<% if s.email[0] == 'a' || s.email[0] == 'A' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>B</dt>
    <dd>
        <ul>
            <% @course.students.each do |s| %>
				<% if s.email[0] == 'b' || s.email[0] == 'B' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>C</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'c' || s.email[0] == 'C' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>D</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'd' || s.email[0] == 'D' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>E</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'e' || s.email[0] == 'E' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>F</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'f' || s.email[0] == 'F' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>G</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'g' || s.email[0] == 'G' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>H</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'h' || s.email[0] == 'H' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>I</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'i' || s.email[0] == 'I' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>J</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'j' || s.email[0] == 'J' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>K</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'k' || s.email[0] == 'K' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>L</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'l' || s.email[0] == 'L' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>M</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'm' || s.email[0] == 'M' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>N</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'n' || s.email[0] == 'N' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>O</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'o' || s.email[0] == 'O' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>P</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'p' || s.email[0] == 'P' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>Q</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'q' || s.email[0] == 'Q' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>R</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'r' || s.email[0] == 'R' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>S</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 's' || s.email[0] == 'S' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>T</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 't' || s.email[0] == 'T' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>U</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'u' || s.email[0] == 'U' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>V</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'v' || s.email[0] == 'V' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>W</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'w' || s.email[0] == 'W' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>X</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'x' || s.email[0] == 'X' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>Y</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'y' || s.email[0] == 'Y' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
    <dt>Z</dt>
    <dd>
        <ul>
           <% @course.students.each do |s| %>
				<% if s.email[0] == 'z' || s.email[0] == 'Z' %>
            		<li><%=link_to s.email, gradeview_path(:id=>s.random_url, :class=>"blue")%></li>
            	<% end %>
            <% end %>
        </ul>
    </dd>
</dl>

  
</body>


</html>